#!/bin/sh
# TODO turn this into an actually working function. . :
#seconds=$SECONDS OR 5
#maxtries=$MAX_TRIES OR 50
#current=0
#until $command;
#do
#    echo "$command failed. Will try again in $seconds"
#    sleep $seconds
#    if current >= max_tries; then
#        echo "ERROR: $command failed $maxtries in a row. Will not try again."
#        exit(1) 
#    fi
#done

